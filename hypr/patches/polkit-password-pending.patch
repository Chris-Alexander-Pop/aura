diff --git a/src/core/PolkitListener.cpp b/src/core/PolkitListener.cpp
index d2bdfa5..1fbc3ac 100644
--- a/src/core/PolkitListener.cpp
+++ b/src/core/PolkitListener.cpp
@@ -495,12 +495,15 @@ void CPolkitListener::handleLine(SActiveAuth& a, const std::string& line) {
     };
 
     if (auto p = prefix("PAM_PROMPT_ECHO_OFF")) {
-        a.helper.gotPrompt = true;
+        a.helper.gotPrompt           = true;
+        a.helper.passwordPromptActive = true;
         g_pAgent->onRequest(*p, false);
+        flushPendingResponse(a);
         return;
     }
     if (auto p = prefix("PAM_PROMPT_ECHO_ON")) {
-        a.helper.gotPrompt = true;
+        a.helper.gotPrompt            = true;
+        a.helper.passwordPromptActive   = false;
         g_pAgent->onRequest(*p, true);
         return;
     }
@@ -544,11 +547,19 @@ void CPolkitListener::handleLine(SActiveAuth& a, const std::string& line) {
     }
 }
 
+void CPolkitListener::flushPendingResponse(SActiveAuth& a) {
+    if (!a.helper.passwordPromptActive || a.helper.pendingResponse.empty() || a.helper.stdinFd < 0)
+        return;
+    const std::string line = a.helper.pendingResponse + "\n";
+    (void)write(a.helper.stdinFd, line.data(), line.size());
+    a.helper.pendingResponse.clear();
+}
+
 void CPolkitListener::submitResponse(const std::string& password) {
     if (!m_current || m_current->helper.stdinFd < 0)
         return;
-    const std::string line = password + "\n";
-    (void)write(m_current->helper.stdinFd, line.data(), line.size());
+    m_current->helper.pendingResponse = password;
+    flushPendingResponse(*m_current);
 }
 
 void CPolkitListener::cancelCurrent() {
@@ -597,6 +608,8 @@ void CPolkitListener::killHelper(SHelperProc& h) {
         h.pid = -1;
     }
     h.buffer.clear();
+    h.gotPrompt            = false;
+    h.passwordPromptActive = false;
 }
 
 void CPolkitListener::completeAuth(bool succeeded, bool cancelled) {
diff --git a/src/core/PolkitListener.hpp b/src/core/PolkitListener.hpp
index e5e3056..3963f44 100644
--- a/src/core/PolkitListener.hpp
+++ b/src/core/PolkitListener.hpp
@@ -54,7 +54,9 @@ class CPolkitListener {
         int         stdoutFd  = -1;
         bool        gotPrompt = false;
         bool        triedFork = false;
+        bool        passwordPromptActive = false;
         std::string buffer;
+        std::string pendingResponse;
     };
 
     struct SActiveAuth {
@@ -74,6 +76,7 @@ class CPolkitListener {
     void        killHelper(SHelperProc& h);
     void        onHelperReadable();
     void        handleLine(SActiveAuth& a, const std::string& line);
+    void        flushPendingResponse(SActiveAuth& a);
     void        completeAuth(bool succeeded, bool cancelled);
     void        startNextQueued();
     void        rejectActive(const std::string& msg);
