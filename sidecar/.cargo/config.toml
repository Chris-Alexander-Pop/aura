# Faster local builds: incremental + LLD linker (Arch: extra/lld).
# sccache: pacman -S sccache — enabled via scripts/rust-cache-env.sh when installed
[build]
incremental = true

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
