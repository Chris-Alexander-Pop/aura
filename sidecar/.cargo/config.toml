# Faster local builds: sccache + incremental + LLD linker (Arch: extra/lld).
# sccache: pacman -S sccache  (scripts/rust-cache-env.sh also sets RUSTC_WRAPPER)
[build]
rustc-wrapper = "sccache"
incremental = true

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
