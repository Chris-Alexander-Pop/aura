# Hyprland bind edge cases for parser tests
# full-line comment above

bind = SUPER, Return, exec, ags msg toggle control-center
bindl = SUPER, mouse:272, movewindow
bindr = , X, killactive
binde = SUPER, left, resizeactive, -20 0
bindm = SUPER, F1, movewindow

# unknown verb — not a bind line
exec = foot

# empty action segments skipped
bind = , Q, killactive
